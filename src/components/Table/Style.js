import { StyleSheet } from "react-native";

export default StyleSheet.create({
  block: {
    marginBottom: 20,
    width: '100%'
  },
  table: {
    width: '100%',
    resizeMode: "contain",
    marginTop: -120
  }
});
